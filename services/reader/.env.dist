# Token with read access to the (private) flat-feeds repo, used by
# gatsby-source-github-repository at build time.
GITHUB_TOKEN=

# Optional: source a non-default branch of flat-feeds (defaults to main).
FEEDS_REF=
