{
  "name": "root",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "lerna run build",
    "test": "lerna run test",
    "deploy": "lerna run deploy"
  },
  "devDependencies": {
    "lerna": "^8.1.3"
  }
}
