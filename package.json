{
  "name": "root",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "lerna run build",
    "test": "lerna run test",
    "deploy": "lerna run deploy"
  },
  "devDependencies": {
    "lerna": "^9.0.0"
  },
  "dependencies": {
    "esbuild": "^0.25.0"
  }
}
