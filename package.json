{
  "name": "root",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "lerna run build",
    "test": "lerna run test",
    "deploy": "lerna run deploy"
  },
  "devDependencies": {
    "lerna": "^9.0.0"
  },
  "dependencies": {
    "esbuild": "^0.27.0"
  },
  "pnpm": {
    "patchedDependencies": {
      "rss@1.2.2": "patches/rss@1.2.2.patch"
    }
  }
}
