{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "deploy": "lerna run deploy"
  },
  "workspaces": [
    "components/*",
    "libraries/*",
    "packages/*",
    "services/*"
  ],
  "devDependencies": {
    "lerna": "^7.0.0"
  }
}
