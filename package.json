{
  "name": "root",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "pnpm -r run --if-present build",
    "test": "pnpm -r run --if-present test",
    "deploy": "pnpm -r run --if-present deploy"
  },
  "devDependencies": {
    "lerna": "9.0.7"
  },
  "pnpm": {
    "overrides": {
      "gatsby-plugin-sass>sass-loader": "^16.0.0"
    }
  }
}
